# Example only - do NOT commit real credentials to source control.
# In a real cluster, this value would come from a managed cloud secret store
# (Azure Key Vault, AWS Secrets Manager, GCP Secret Manager) via a CSI Secret Store
# driver or the External Secrets Operator, or from your CI/CD pipeline's secret
# injection - never a checked-in YAML file with real values. See README "Secrets in
# a real deployment" for the full explanation.
apiVersion: v1
kind: Secret
metadata:
  name: car-booking-db-credentials
type: Opaque
stringData:
  password: "REPLACE_ME"
